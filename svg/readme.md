It's a simple script to convert an image to an svg.

# Usage:

1. Install requirements "pip install -r requirements.txt"
2. Add filepath to 'src_path' 
3. Run the code "python convert.py"
4. The svg will be saved in the same directory as the filename, sharing the same name.